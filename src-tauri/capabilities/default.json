{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main MultiSerial window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
