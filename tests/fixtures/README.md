# Test Fixtures

Store shared frontend, E2E, and Rust integration fixtures here.

- Keep generated output out of this directory.
- Prefer small deterministic byte samples for serial input/output tests.
- Put large or machine-generated artifacts under `.dev-data/` instead.
